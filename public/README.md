# public/

Place the hero image here as **`hero.png`** — the party photo that already
contains the neon **"SIIRI 45"** sign. The page references it as `/hero.png`.

If the file is missing, the app degrades gracefully to a tasteful emerald/gold
placeholder block, so the build and page never break.
